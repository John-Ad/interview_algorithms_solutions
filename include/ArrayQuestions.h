#include "include.h"

void getMissingNum(int* arr);
void findNumOfPairs();                           // find num of pairs where i<j and elem[i] != elem[j]
void findNumOfOpsForPalindrome(vector<int> arr); //find the number of operations needed to turn arr into a palindrome
void findNumOfPairsBetweenTwoNums(int l, int r); // find num of pairs between l and r
